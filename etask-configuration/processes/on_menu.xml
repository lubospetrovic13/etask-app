<document xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://petriflow.com/petriflow.schema.xsd">
	<id>onboarding/on_menu</id>
	<version>1.0.0</version>
	<initials>ONM</initials>
	<title name="net_title">Onboarding menu</title>

	<!--
		Zdroj je po anglicky, slovencina je preklad. Kluc je atribut `name` na
		prelozitelnom elemente; bez oboch polovic sa zobrazi povodna hodnota
		a nic to nepovie. Kod jazyka musi byt DVOJPISMENOVY - `sk-SK` sa
		naimportuje a nikdy sa nepouzije (ENGINE_ISSUES E15).

		Nazvy zobrazeni su v Groovy akcii nizsie, kde `pfi18n` nedosiahne -
		tam su dvojjazycne cez `i18n("English", ["sk": "Slovensky"])`.
	-->
	<i18n locale="sk">
		<i18nString name="net_title">Menu Nástupy</i18nString>
		<i18nString name="hr_title">Personalista</i18nString>
		<i18nString name="t_on_menu_label">Stav menu</i18nString>
	</i18n>
	<icon>menu</icon>
	<defaultRole>false</defaultRole>
	<anonymousRole>false</anonymousRole>
	<transitionRole>false</transitionRole>
	<!--
		Karta „Nástupy zamestnancov" v bočnom menu sama žiadne zobrazenia
		nenesie - uzol URI vznikne importom siete s identifikátorom
		`onboarding/...`, ale to, čo pod kartou vidno, sú prípady procesov
		`filter` a `preference_filter_item`. Bez nich karta vedie do prázdna.

		Túto sieť drží pri živote jediný bootstrap case (processes.json ->
		bootstrapCase s `rebuildOnNewVersion`), z ktorého sa spustí akcia nižšie.

		Každý vzor tu je kvôli konkrétnej pasci:

		  * `createOrUpdateMenuItem` sa volá s DEVIATIMI argumentmi. Existuje
		    dvakrát - projektová (7-9) a enginová (11) - a rozlišujú sa len
		    aritou. Pri ôsmich si Groovy vyberie enginovú a padne to.
		  * Jej UPDATE cesta nefunguje, takže položka sa vždy zahodí a postaví
		    znova. PORADIE: najprv `deleteMenuItem`, POTOM `deleteFilter` -
		    naopak to padne, lebo deleteMenuItem si filter ešte raz načíta.
		  * `nets` (allowedNets) musí byť aj na zobrazeniach, z ktorých sa
		    zakladať nemá: nesie tlačidlo „+" AJ ponuku stĺpcov. Bez neho zoznam
		    ukáže iba `meta-*` stĺpce - a nič sa nezaloguje.
		  * URI cesta MUSÍ sedieť s kľúčom v `uriNodes` v processes.json, inak
		    položka vznikne bez karty, na ktorej by bola vidno.

		Päť zobrazení - presne tá tabuľka „čo má kto vidieť" zo zadania:

		  kto            zobrazenie               ako je zúžené
		  personalista   Onboarding requests      všetko, čo smie vidieť
		  personalista   Drafts and returns       taskIds:"t_on_request"
		  vedúci         To approve               taskIds:"t_on_approval"
		  IT správca     Accounts to create       taskIds:"t_on_accounts"
		  všetci         Completed onboardings    dataSet.on_status.keyValue

		Zobrazenia sú typu **Case**, nie Task: Task zobrazenie ukáže riadok na
		úlohu, takže tá istá žiadosť by v zozname bola aj viackrát.

		Cena za to je, že Case zoznam sa filtruje oprávnením `view` na prípade,
		nie právom prechod vykonať - personalista má `view` na všetky žiadosti,
		takže by si ich videl aj v „To approve". Preto majú tie položky `roles`
		nastavené na rolu, ktorej patria: drawer aj počítadlá ich ostatným
		vôbec nezobrazia. Nie je to bezpečnostná hranica (tou je `perform` na
		prechode a `userRef` na vybranom vedúcom), je to poriadok v menu.

		Filtruje sa dvoma spôsobmi a oba sú tu zámerne:

		  * podľa **polohy tokenu** - `taskIds` drží id práve otvorených
		    prechodov, teda „čo sa s prípadom dá robiť";
		  * podľa **dátového poľa** - „Completed" hovorí o stave žiadosti, nie
		    o tom, ktorá úloha je zhodou okolností otvorená. Ide to, lebo
		    `on_status` má `immediate="true"`. KĽÚČ je v `.keyValue`;
		    `.textValue` drží PRELOŽENÉ POPISKY (všetky jazyky naraz), takže
		    dopyt na kľúč cez `.textValue` nenájde NIKDY NIČ a nič to nepovie.
	-->
	<caseEvents>
		<event type="create">
			<id>on_menu_create</id>
			<actions phase="post">
				<action id="1"><![CDATA[
def created = []
def unchanged = []
def failed = []

def siet = "onboarding/on_request"
def uzol = "onboarding"
def vsetky = "processIdentifier:\"" + siet + "\""

// Predvolene stlpce zoznamu. Su to `HeaderColumn.uniqueId` oddelene ciarkou:
// meta stlpec je `meta-<pole>`, datove pole je `<identifikator siete>-<id pola>`
// a MUSI mat immediate="true". Zadanie chce meno nastupujuceho (nesie ho nazov
// pripadu), datum nastupu, stav a kto schvaluje.
def stlpce = "meta-title" +
        "," + siet + "-on_start_date" +
        "," + siet + "-on_status" +
        "," + siet + "-on_approver_name"

def views = [
        // Nazov prveho zobrazenia je TITULOK SIETE, nie kopia toho retazca.
        // Titulok siete je I18nString a ma svoj preklad v bloku `<i18n>`, takze
        // sa preklada na jednom mieste; kopia by znamenala prelozit to dvakrat
        // a druhy vyskyt by nikto nenasiel.
        [id: "on_all", name: petriNetService.getNewestVersionByIdentifier(siet).title,
         type: "Case", icon: "how_to_reg",
         query: vsetky, nets: [siet], roles: [:], headers: stlpce],
        // Nazov nesie DOMENU appky. Polozky menu su pripady jednej siete pre
        // cely portal a nazov v nich unikatny nie je - dve appky s polozkou
        // „To approve" sa na dashboarde (kde nie su priecinky) nedaju odlisit
        // a kod, ktory si polozku hlada podla nazvu, dostane tu druhu.
        [id: "on_drafts",
         name: i18n("Onboarding drafts and returns", ["sk": "Rozpísané a vrátené nástupy"]),
         type: "Case", icon: "edit_note",
         query: vsetky + " AND taskIds:\"t_on_request\"",
         nets: [siet], roles: ["hr": siet], headers: stlpce],
        [id: "on_to_approve",
         name: i18n("Onboardings to approve", ["sk": "Nástupy na schválenie"]),
         type: "Case", icon: "how_to_reg",
         query: vsetky + " AND taskIds:\"t_on_approval\"",
         nets: [siet], roles: ["approver": siet], headers: stlpce],
        [id: "on_accounts",
         name: i18n("Accounts to create", ["sk": "Účty na založenie"]),
         type: "Case", icon: "manage_accounts",
         query: vsetky + " AND taskIds:\"t_on_accounts\"",
         nets: [siet], roles: ["it_admin": siet], headers: stlpce],
        // Podla KLUCA moznosti, nie podla popisku: stav je `enumeration_map`,
        // aby sa dal prelozit - a kluc je nezavisly od jazyka aj od
        // preformulovania popisku.
        [id: "on_completed",
         name: i18n("Completed onboardings", ["sk": "Dokončené nástupy"]),
         type: "Case", icon: "task_alt",
         query: vsetky + " AND dataSet.on_status.keyValue:\"done\"",
         nets: [siet], roles: [:], headers: stlpce],
]

views.each { v ->
    def id = v.id as String
    try {
        def existing = menu_item(id)
        if (existing != null) {
            // Porovnava sa dopyt A allowedNets - oboje ide do konstruktora
            // a na existujucej polozke sa zmenit nedaju.
            def current = getFilterFromMenuItem(existing)
            def currentQuery = ((current?.dataSet?."filter"?.value ?: "") as String)
            def currentNets = ((current?.dataSet?."filter"?.allowedNets ?: []) as List)
                    .collect { it as String } as Set
            def wantNets = (v.nets as List).collect { it as String } as Set
            // Do porovnania patri aj NAZOV, vratane prekladov. Bez toho by uz
            // nasadena instancia zmeneny nazov (a doplneny preklad) nikdy
            // nedostala: polozka tam je s tym istym dopytom, takze by sa vzdy
            // vyhodnotila ako nezmenena.
            def currentName = current?.dataSet?."i18n_filter_name"?.value
            def sameName = currentName != null &&
                    (currentName.defaultValue as String) == (v.name.defaultValue as String) &&
                    (currentName.translations ?: [:]) == (v.name.translations ?: [:])
            if (currentQuery == (v.query as String) && currentNets == wantNets && sameName) {
                nastav_zobrazenie(existing, v.nets as List, v.headers)
                // Uzol dorovnaj AJ pri nezmenenej polozke: URI uzly zije
                // Elasticsearch, polozky menu Mongo. Po vymene ES indexu maju
                // uzly nove id, polozka visi na starom a priecinok v bocnom
                // paneli je PRAZDNY - bez chyby a bez logu (RUNBOOK 4).
                pripoj_do_uzla(existing, uzol)
                unchanged << id
                return
            }
            deleteMenuItem(existing)
            if (current != null) {
                deleteFilter(current)
            }
        }
        def item = createOrUpdateMenuItem(
                id,                 // menu_item_identifier
                uzol,               // URI cesta - musi sedet s uriNodes
                v.type as String,
                v.query as String,
                v.icon as String,
                v.name,             // I18nString - polozka menu vie byt dvojjazycna
                v.nets as List,     // allowedNets - bez toho ziadne stlpce
                v.roles as Map,     // allowed_roles - kto polozku v menu vidi
                [:]                 // bannedRoles - dourcuje aritu na projektovu
        )
        if (item) { created << id } else { failed << id }
        nastav_zobrazenie(item, v.nets as List, v.headers)
    } catch (Exception e) {
        failed << (id + " (" + e.getClass().simpleName + ": " + e.message + ")")
    }
}

def summary = "Onboarding menu - " + (created.size() + unchanged.size()) + "/" + views.size()
if (failed) {
    summary = "ERROR onboarding menu - failed: " + failed.join(", ")
}
changeCaseProperty("title").about { summary }
				]]></action>
			</actions>
		</event>
	</caseEvents>
	<function scope="process" name="nastav_zobrazenie"><![CDATA[
{ def menuItem, def nets, def headers ->
    // `enable_case_title`  - vypnuty dialog "Vyplnte nazov pripadu" pri "+".
    // `default_headers`    - predvolene stlpce zoznamu.
    // Ani jedno nie je argument `createOrUpdateMenuItem`.
    if (menuItem == null) return
    def payload = [:]
    if (nets) {
        payload.put("enable_case_title", ["value": false, "type": "boolean"])
        payload.put("case_require_title_in_creation", ["value": false, "type": "boolean"])
    }
    if (headers) {
        payload.put("default_headers", ["value": headers as String, "type": "text"])
    }
    if (payload) {
        setData("view", menuItem, payload)
    }
}
	]]></function>
	<function scope="process" name="menu_item"><![CDATA[
{ def identifier ->
    // `findMenuItem(id)` z ActionDelegate existujucu polozku nenajde (hlada
    // v inom rozsahu), takze by sa pri kazdom spusteni zalozila druha sada.
    def id = ((identifier ?: "") as String).trim()
    if (!id) return null
    def items = findCases { it.processIdentifier.eq("preference_filter_item") }
    return items?.find { ((it.dataSet["menu_item_identifier"]?.value ?: "") as String) == id }
}
	]]></function>
	<roleRef>
		<id>hr</id>
		<caseLogic>
			<create>true</create>
			<view>true</view>
		</caseLogic>
	</roleRef>
	<role>
		<id>hr</id>
		<title name="hr_title">HR officer</title>
	</role>
	<transition>
		<id>t_on_menu</id>
		<x>260</x>
		<y>140</y>
		<label name="t_on_menu_label">Menu state</label>
		<icon>menu</icon>
		<assignPolicy>auto</assignPolicy>
		<roleRef>
			<id>hr</id>
			<logic>
				<perform>true</perform>
			</logic>
		</roleRef>
	</transition>
	<place>
		<id>p_alive</id>
		<x>80</x>
		<y>140</y>
		<label>Alive</label>
		<tokens>1</tokens>
		<static>false</static>
	</place>
	<arc>
		<id>a_on_menu</id>
		<type>read</type>
		<sourceId>p_alive</sourceId>
		<destinationId>t_on_menu</destinationId>
		<multiplicity>1</multiplicity>
	</arc>
</document>
